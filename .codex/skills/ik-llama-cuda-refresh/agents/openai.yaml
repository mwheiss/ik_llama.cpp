interface:
  display_name: "ik_llama CUDA Refresh"
  short_description: "Refresh, replay local patches, and build"
  default_prompt: "Use $ik-llama-cuda-refresh to update ik_llama.cpp, preserve the CUDA graph cleanup API when needed, replay the runtime RoPE frequency-scale override only while it remains clean, and rebuild it."
